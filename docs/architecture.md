# NovaOS Architecture

NovaOS uses the Linux kernel for hardware scheduling, memory management, networking, drivers, and security primitives. Project-owned layers provide initialization, services, package metadata, desktop shell, settings, notifications, and system applications.

The design combines a familiar centered desktop workflow with Linux transparency and composability.
